# Claude Code local config (contains secrets)
.remember/
.claude/settings.local.json

# Credentials and key material — must never be committed
rclone.conf
*.jwe
*.age
secrets/
.env
.env.*

# Docker local overrides
docker-compose.override.yml

# SQLite databases (manifest DB is runtime data, not source)
*.db
*.sqlite
*.sqlite3

# Python build artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
dist/
build/

# macOS
.DS_Store
